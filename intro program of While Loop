#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    
    // Your code here
    int count = 2;
    while (count<=n) {
        printf("%d ",count);
        count = count + 2;
    }
    return 0;
}
